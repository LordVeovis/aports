--- a/src/logger.cc
+++ b/src/logger.cc
@@ -83,7 +83,8 @@
 std::string logger::err()
 {
     char buf[2048];
-    return strerror_r(errno, buf, sizeof(buf));
+    strerror_r(errno, buf, sizeof(buf));
+    return buf;
 }
 
 logger logger::error()
